function plantingController(repository) {
    var _repository = repository;

    var planting = {
        load: function(id) {
            repository.db.
        },
        save: function () { },
        create : function() {}

    };
    return planting;
}